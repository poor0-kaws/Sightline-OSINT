// We isolate API calls here so the rest of the UI does not care how transport works.
export async function fetchDashboardData() {
  // TODO [CORE]: call your real backend endpoints and normalize the response for the UI.
  return {
    status: "stub",
  };
}

